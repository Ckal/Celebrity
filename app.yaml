name: Celebrity
type: gradio-space
sdk: gradio

metadata:
  space_id: Chris4K/Celebrity
  original_url: https://huggingface.co/spaces/Chris4K/Celebrity

deploy:
  targets:
    - public-repo
    - huggingface

build:
  command: bash build.sh

test:
  command: echo "Tests not yet implemented"
